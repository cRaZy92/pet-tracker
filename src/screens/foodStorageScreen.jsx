import { StyleSheet, Text, View } from 'react-native';

export default function FoodStorageScreen() {
  return (
    <View style={styles.container}>
      <Text>WIP</Text>
      <Text>Display all available food with amount.</Text>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#fff',
    alignItems: 'center',
    justifyContent: 'center',
  },
});
