import { StyleSheet, Text, View } from 'react-native';

export default function FoodTypeCreateScreen() {
  return (
    <View style={styles.container}>
      <Text>WIP</Text>
      <Text>Form to add new food type</Text>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#fff',
    alignItems: 'center',
    justifyContent: 'center',
  },
});
